export class HttpClient {
  // Foundation for global API client with retry and error contract
}

export class StreamingClient {
  // Foundation for JSON-LD chunk parsing and stream abortion
}

export class GenerationRepository {
  // Foundation for generation abstraction layer
}
